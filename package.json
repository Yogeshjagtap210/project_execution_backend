{
  "name": "project-execution-board-backend",
  "version": "1.0.0",
  "description": "Fullstack Application Service for Project Execution Board",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
